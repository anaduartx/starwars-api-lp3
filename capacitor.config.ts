import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicAPI-2408',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
